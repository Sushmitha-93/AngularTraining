import { MyHilighterDirective } from './my-hilighter.directive';

describe('MyHilighterDirective', () => {
  it('should create an instance', () => {
    const directive = new MyHilighterDirective();
    expect(directive).toBeTruthy();
  });
});
